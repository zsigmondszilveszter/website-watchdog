{
  "name": "website_watchdog",
  "version": "1.0.0",
  "description": "This script runs periodically and watch if a given website is available or not. If the site is not available than sends an email to given email address",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Szilveszter Zsigmond",
  "license": "ISC",
  "dependencies": {
    "node-libcurl": "^1.0.0",
    "nodemailer": "^2.6.4"
  }
}
